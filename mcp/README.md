# MCP

`mcp/` is reserved for Model Context Protocol integration work.

## Goal
- Provide an MCP server bridge for Elastisched tooling.
- Support local model hosts (for example, Ollama) and related runtime adapters.

Status: planning stage (see [`mcp/TODO.txt`](TODO.txt)).
