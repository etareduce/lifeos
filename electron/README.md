# Electron

`electron/` is a lightweight desktop wrapper around the web app.

## Files
- [`electron/main.js`](main.js): Electron entrypoint.
- [`electron/package.json`](package.json): scripts and dependencies.

## Run
1. `cd electron`
2. `npm install`
3. `npm run dev`
